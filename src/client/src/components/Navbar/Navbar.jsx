import "./Navbar.css";

import React from "react";

const Navbar = () => {
  return (
    <nav>
      Header
      <div className="nav-button-cont">
        <button>Login</button>
        <button>Register</button>
      </div>
    </nav>
  );
};

export default Navbar;
